# ЭкоДок — зависимости
# Ядро
PyMuPDF>=1.23        # парсинг PDF (fitz)
python-docx>=1.1     # чтение/генерация .docx
openpyxl>=3.1        # генерация .xlsx (печатные формы)
lxml>=5.0            # генерация/валидация XML по XSD

# OCR для сканов/jpg (опционально — модуль грузится лениво)
# Требует установленного Tesseract-OCR + русский языковой пакет (rus).
pytesseract>=0.3.10
Pillow>=10.0

# PDF-экспорт печатных форм через MS Office (опционально; иначе LibreOffice)
pywin32>=306; sys_platform == "win32"

# ИИ-анализ (ecodoc ai / intake --ai) работает на stdlib — ключи задаются
# переменными окружения, локальный Ollama обнаруживается сам (ecodoc ai setup).
