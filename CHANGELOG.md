# История версий

## 0.58.0 — 2026-08-22

**Разработка = полноценные документы по структуре настоящих.** Пользователь
положил в «Формы» реальные проекты (НДВ Паркинг 234 с. и Протелюкс 1320 с.,
разделы ООС АК-01-25 337 с. и Выборжец, отчёт инвентаризации Протелюкс
609 с., принятые планы НМУ Москвы и СПб, помесячные журналы ВСМ, скан
2-ТП водхоз, 18 паспортов/протоколов) и попросил «чтобы не листик, а
настоящий отчёт». Каждый генератор сверен с реальным документом и
действующим НПА (с региональными требованиями) и переписан.

- **Проект НДВ** (`development/ndv_project.py`) — состав по п. 11 методики
  № 581 и структуре «Книги 2» Паркинга: состав проекта, разработчик и
  исполнители, аннотация, разделы 1–6, 21 таблица с дословными шапками
  (перечень ЗВ с ПДК/ОБУВ/классом, параметры ИЗАВ, метео, расчётные точки,
  максимальные концентрации, вклады источников, НДВ по годам/источникам/
  объекту, НМУ +20/40/60 %, план-график контроля), 13 приложений; выгрузки
  «Эколога» многолистовой книгой (источники/концентрации/точки/вклады);
  требования ФБУЗ Москвы учтены.
- **Раздел ООС** (`development/oos.py` + `oos_structure.py` +
  `oos_waste_calc.py`) — том по структуре АК-01-25: 13 разделов с
  подразделами (природные условия по ИЭИ, проектные решения, воздух —
  строительство и эксплуатация, вода с балансом, шум, отходы с расчётом
  нормативов образования по типовым формулам — сверено на числах эталона,
  плата по ставкам года, ПЭК/ПЭМ), 43 таблицы с дословными заголовками,
  выводы по разделам как у Выборжца, литература, приложения.
- **Отчёт об инвентаризации выбросов** (`air_inventory_docx.py`) — по
  приложению 4 к № 871 и отчёту Протелюкс: титул с грифом, разработчик,
  введение с таблицей 1, разделы 1–7, таблицы 3.1/3.2/3.6/3.7/2.1/1.1–1.3
  дословно; xlsx-таблицы сохранены.
- **План НМУ — три варианта по региону объекта** (`nmu_regions.py`):
  Москва по 231-ПП (8 граф как в согласованном ДПиООС плане, пояснительная
  записка п. 17, план-график прил. 3, журнал прил. 1, состав заявления),
  Санкт-Петербург (9 граф с эффектом %, блок ответственного — как в
  принятом перечне), федеральный № 662.
- **Журнал 1028** — помесячный режим как у ВСМ (`extra.period_month`),
  сборка года из месяцев, все четыре квартальных бланка (Б2/Б3/МО/У)
  заполняются как в PDF (нули в пустых графах, заглушка Прил. 4).
- **2-ТП (водхоз)** — печать 4 блоками граф как в сданном скане Протэлюкс.
- **Паспорт отходов** — состав из протокола КХА (`lab_results`), лист
  «Основание для определения состава» с реквизитами протокола/лаборатории/
  аттестата, дата утверждения в грифе; справка о V классе по акту
  биотестирования (`generate_v_class`).
- **Папка «Формы» добрана** официальными текстами: 4-ОС (бланк 2026 +
  приказы № 346/566/734), 2-ТП воздух (xls РПН), водхоз (бланк № 445 и
  ред. № 473), кадастр СПб (типовые формы), № 158, № 117, ПП № 2451/684,
  № 1021 (ПНООЛР — не 1029!), № 1118, № 109, № 173/262, № 182/241, № 227,
  № 286 — в каждой папке ИСТОЧНИК.txt.
- Реестр форм видит подпапки по регионам/объектам; слоты водхоз/ПЛАРН/
  ДВОС/ИЭИ/Оценка; реестр НПА: ПНООЛР № 1021, 4-ОС с изм. 566/734.

- **4-ОС** — изменения № 566 (порог 100 тыс. ₽, круг респондентов) и
  № 734 (ОКВЭД-исключения по стр. 04/07), целые тысячи как в XML websbor,
  23 контрольных соотношения, подписной блок по бланку.

Тесты: 486 passed. install.bat НЕ нужен.

## 0.57.0 — 2026-08-22

**Все генерации сверены с бланками и действующими НПА и переписаны.**
По жалобе «генерации неправильные, хотя образцы были приложены»: 13 форм
сверены пографно с принятыми отчётами и текстами приказов (с цитатами-
доказательствами) — расхождения нашлись в КАЖДОЙ, всего 96, все закрыты.
Системная причина: формы писались по отчётам 2021 г., а НПА с тех пор
менялись — и ещё три меняются с 01.09.2026.

- **Декларация о плате за НВОС** — итоговая часть по действующей ред. № 241
  (020 = 021…028; блоки 130–145 побочные продукты/породы/животноводство;
  150 мероприятия, 160 корректировка, 170 к внесению, 180 зачёт — раньше
  суммы уезжали в чужие строки); разделы 4–10 отдельными листами по графам
  бланка; с отчёта за 2026 г. — форма приказа № 182 (раздел 10
  искусственные грунты); титул дословно, ТО РПН по региону, КБК 20 цифр.
- **Отчёт ПЭК (№ 173 ред. 262)** — таблицы 2.1/2.2/2.3/3.1/3.2/5.1/6.1
  приведены к составу граф приказа (были выдуманные графы), 4.2 гр. 20/21
  хранение/накопление, титул по форме, названия таблиц дословно.
- **2-ТП (воздух)** — убраны графы ПДВ/ВСВ из раздела 1 (их нет в № 661),
  код NOx 0012 (был 0301), раздел 2 без SO2/CO/NOx + строка 8888, код ОНВ
  в шапках и XML, строки 302/303.
- **2-ТП (отходы), № 614** — убраны строки «ВСЕГО» из раздела I, сортировка
  по классу, гр. 27/28 хранение/захоронение, контроль назначения передачи,
  титул/подпись/шапка дословно.
- **Журнал учёта отходов** — блок граф 8–16 бланка теперь заполняется
  (раньше оставался пустым с чужой строкой образца), межлистовые ссылки
  затираются значениями, «Перечень…» (ред. № 825), период «1 кв 2025 года»,
  календарь ежемесячно; с 01.09.2026 — форма приказа № 227.
- **План НМУ (№ 662/651)** — графа «выброс после» больше не считается по
  нормативному % (он о вкладах в концентрацию, а не о г/с), мероприятия
  только для контролируемых веществ, запреты дословно п. 9, ровно 9 пунктов.
- **Паспорт отходов** — с 01.09.2026 форма приказа № 286 (7 строк сведений
  о лице, новые формулировки), агрегатное состояние по коду ФККО.
- **2-ТП (водхоз)** — ред. приказа № 473 от 06.08.2026: 5+3+1 пар граф
  32–49 (были сдвинуты), «N строки», коды очистки/категорий, точность.
- **ПЛАРН** — ред. ПП № 684 с 01.09.2026: учения раз в 5 лет, 14 дней на
  уведомление, сорбент, приложения, адресаты и состав оповещения.
- **ДВОС (№ 117)** — все 7 разделов по форме приложения (были пропущены
  графы ОКПД2, источники финансирования, класс опасности, ГРОРО…).
- **Кадастр СПб** — район в п. 2.2 (иначе сведения «не представлены»),
  ФККО с пробелами, класс римской, сетка Формы 4 как в типовой.
- **Программа ПЭК** — содержание по пп. 3–9 Требований № 109 (орган,
  источник × вещество, очистные, численность, методики измерений).
- **«Отчётность III кат./МСП»** — такой формы в законе нет с 2019 г.:
  переименована в справку для раздела 4 отчёта ПЭК, убрана из ЛКПП и из
  ложных обязанностей календаря.
- **Санитар данных**: сверка «код ↔ название» по официальному перечню
  199 веществ (ловит «0325 Керосин» — 0325 это мышьяк); виды стоков не
  попадают в сбросы; конфликты масс показываются, а не глотаются.
- Реестр НПА для автопроверки бланков актуализирован; ФККО — 9146 кодов
  по ред. приказа № 341 от 08.06.2026 (актуален).

Тесты: 435 passed. install.bat НЕ нужен.

## 0.56.0 — 2026-08-13

Контур «Разработка» доведён до полноценных документов: четыре генератора,
каждый сверен с действующими НПА по открытым источникам (двое из них поймали
смену нормативки, которую мы бы иначе пропустили).

- **План НМУ** — переписан с нуля: приказ № 811, по которому он строился,
  УТРАТИЛ СИЛУ с 01.03.2026. Теперь план по действующим приказам Минприроды
  № 662 (состав и форма плана, сверено с официальным сканом) и № 651
  (целевые проценты снижения по степеням опасности НМУ: ≥15/≥20/≥40 %,
  для ТЭК/ЖКХ — свои). Таблицы мероприятий строятся из ваших источников
  выбросов (источник × вещество, «до» и «после» в г/с), сводный расчёт
  снижения по веществам, вид прогноза — по категории объекта. Было
  17 абзацев без таблиц — стало 151 абзац и 6 таблиц.
- **Программа ПЭК** — по приказу № 109 в действующей редакции от 12.05.2025
  (включая разделы о побочных продуктах и искусственных грунтах из ТКО):
  все разделы приказа, план-графики контроля источников выбросов и сточных
  вод с периодичностью из НПА (сбросы: I–II кат. — ежемесячно, III — раз
  в квартал; очистные — 2 раза в год). Было 16 абзацев — стало 54 абзаца
  и 7 таблиц.
- **ПЛАРН** (новый) — по ПП РФ № 2451 в редакции от 21.02.2026, с учётом
  изменений с 01.09.2026 (отходы ликвидации и сорбенты, оповещение за
  30 минут). Максимальный расчётный объём разлива считается по правилу
  «100 % наибольшей ёмкости», автоматический вывод «план обязателен/нет»
  по порогам 3 т (суша) / 0,5 т (вода); таблицы ёмкостей, сценариев,
  оповещения; отходы ликвидации с подбором кодов по действующему ФККО.
- **ДВОС** (новый, объекты II категории) — форма по приказу № 509 утратила
  силу с 01.09.2025: декларация построена по действующему приказу № 117
  от 19.03.2025, включая новый раздел VI (отчётный год + декларируемые
  показатели на 7 лет). Выбросы, сбросы, отходы по классам — из базы.

Все четыре подключены в «Разработке» (кнопка «Сгенерировать (.docx)» +
список «чего не хватает»). Правило прежнее: из данных — всё, что можно;
чего машина знать не может — пометка «[требуется: …]», ничего не
выдумывается.

Тесты: 361 (+33). install.bat НЕ нужен.

## 0.55.0 — 2026-08-13

Все 34 подтверждённых расхождения генераций с бланками закрыты (8 форм,
по агенту-исполнителю на форму; спецификации — вердикты скептиков, каждый
проверен по скачанным НПА и вашим образцам). Плюс закрыты две дыры, через
которые мусор попадал в базу в обход санитара.

### Формы — по действующим бланкам

- **Журнал 1028**: Таблица 4 приведена к действующей редакции (приказ № 825,
  с 01.09.2024) — 13 граф, устаревшая графа «для накопления и последующей
  передачи…» удалена; тоннаж всюду с точностью 3 знака (в бланк уезжали
  хвосты вида 469.0220892119701); заполнение ВАШЕГО бланка из «Форм»
  вычищено: чужие данные образца (отходы и контрагенты ЭталонАктива) больше
  не утекают в документ клиента, при нехватке строк таблица раздвигается,
  сбой бланка виден предупреждением, а не глотается молча.
- **2-ТП (отходы)**: раздел II — по бланку регоператора ТКО (29 своих граф
  тремя подтаблицами с шапкой А|Б|В|Г); код ФККО в графе В — в каноническом
  виде с пробелами «7 33 100 01 72 4» (в XML — без пробелов); формулировки
  раздела III и заголовок раздела I — дословно по печатной форме ЛКПП;
  на титуле оба срока (1 февраля и 15 марта).
- **Декларация**: титул — все 17 полей бланка (исполнитель, главбух, отметки
  о представлении); новые листы «Информация о суммах платы» (по объектам,
  с ИТОГО) и «Авансовые платежи» (строки 010–060, способ исчисления с
  отметкой X); итоговая часть стр.2 — по нумерации бланка (140–195: плата
  с корректировкой, к внесению, зачёт, авансы поквартально, итог, возврат);
  раздел 1 — 18 граф бланка, одна строка на вещество.
- **Отчёт ПЭК**: печатная форма — титул + все 20 таблиц приказа № 173
  (1.1–6.2, включая побочные продукты и искусственные грунты из ТКО);
  таблица 4.2 (движение отходов) — 21 графа, 4.3 (контрагенты) — 12 граф;
  пустые таблицы печатаются с шапкой и прочерками, как в принятых отчётах.
- **Кадастр СПб**: форма 3 — раздельные графы передачи регоператору ТКО и
  прочим (по фактическому приёмщику), графы «в другие субъекты РФ» с целью
  передачи; формы 2 и 4 — шапки и подписи граф по принятому Комитетом отчёту.
- **2-ТП (воздух)**: все 5 разделов бланка — строки 101–109 с графами потока
  очистки (2–7) и нормативами ПДВ/ВСВ (8–9, «X» где не предусмотрено),
  разделы 4 (мероприятия) и 5 (группы источников); контрольные соотношения
  Указаний проверяются.
- **2-ТП (водхоз)**: адресная часть, состав граф разделов 1–2 и помесячная
  разбивка — по форме приказа № 445.
- **Отчётность III категории**: баланс учитывает остатки на начало года
  раздельно «хранение»/«накопление» — ложные предупреждения на согласованных
  данных ушли.

### Мусор больше не просачивается в базу

- Путь кандидатов («Найденные данные» → «Взять») теперь проходит санитар:
  потоки стоков, «работы», группы суммации и коды вне ФККО отбраковываются
  ещё до попадания в кандидаты, коды нормализуются («301» → «0301»).
- «Задание ИИ» прогоняет ответ модели через тот же санитар и пишет в отчёт,
  что отбраковано.
- Акты без кода ФККО сливаются без учёта регистра имени («лом…» и «Лом…» —
  один отход).
- Устаревший `ФККО.xls` в «Формах» (снимок отменённого каталога 2014 года)
  больше не затирает свежий каталог программы; стартовая плашка предупреждает,
  что файл устарел и его можно удалить.

Тесты: 328 (+43 страховки). Сквозная генерация всех 9 форм на трёх реальных
площадках — 0 ошибок; заполнение реального бланка Б2 журнала — без утечек
и падений. install.bat НЕ нужен.

## 0.54.1 — 2026-08-12

Итог контрольной сверки ставок независимым проверяющим: все внедрённые
значения подтверждены (0 расхождений на ~420 сверенных чисел), но найдены
три ошибки в самом расчёте и в старой таблице.

- **ТКО переплачивались на 4,5 %**: к их ставке применялся дополнительный
  коэффициент 1,045, хотя он установлен для ставок Распоряжения № 1852-р,
  а ставка ТКО — из отдельного ПП № 595. Теперь 99,30 ₽/т ровно.
- **Ставка V класса для перерабатывающей промышленности была недостижима**:
  такие отходы считались как «прочие» — 26,12 вместо 60,55 ₽/т (вдвое
  дешевле). У WasteFlow появилось поле `industry` («добывающая» /
  «перерабатывающая»), у V класса теперь работают все три ставки.
- **Ошибки в таблице ставок 2018 года** (по ней считаются 2018–2024):
  «Сухой остаток» стоял 0,0 вместо 0,5 — плата не начислялась вовсе;
  «Аммоний-ион» 1296,4 вместо 1190,2; «БПК полный» 243,2 вместо 243,0;
  строка «Углерод (сажа)» убрана — в ПП РФ № 913 её нет, там стояла
  ставка взвешенных веществ.
- В справочник ставок 2026–2030 добавлены коды веществ (68 позиций
  воздуха и 5 воды) — раньше сопоставление шло только по наименованию.

Тесты: 285.

## 0.54.0 — 2026-08-12

Итог сплошного аудита программы и сверки каждой генерации с вашими бланками:
52 агента-ревьюера, 47 подтверждённых находок; отдельно — 39 агентов сверили
формы с образцами из папки «Формы» и принятыми отчётами (23 расхождения).

### Плата за НВОС считалась неверно — исправлено

- **Коды веществ без ведущего нуля не находили ставку.** В базе они хранятся
  как «301», в справочнике — «0301», и плата обнулялась. На реальной базе
  выходило 446 ₽ вместо 7 668 ₽ — занижение в 17 раз.
- **Ставки заменены на официальные.** Прежде считалось «ставка 2018 года ×
  коэффициент индексации» по 8 веществам воздуха и 5 воды. Теперь в программе
  полные перечни из действующих актов: 2025 — Распоряжение № 1852-р
  (199 веществ воздуха, 173 воды) с доп. коэффициентом 1,045 (ПП № 1034,
  и он не применяется к позициям, перенесённым из ПП № 492); 2026–2030 —
  Распоряжение № 2409-р в ред. № 4110-р. Тексты взяты со сканов
  publication.pravo.gov.ru и вычитаны.
- **ТКО считались как обычные отходы IV класса.** У них своя ставка:
  99,30 ₽/т на 2025 (ПП № 595) и 190 ₽/т на 2026 против 1001,43 и 1088,3.
- **У сажи стояла ставка взвешенных веществ** (36,6 вместо 209,59) —
  занижение почти вчетверо.
- Ставка не найдена по коду — ищется по наименованию (у сбросов свой
  перечень кодов, из-за чего плата за сбросы всегда была нулевой);
  ставка 0 в справочнике больше не считается найденной; при незаполненном
  отчётном годе программа предупреждает, а не подставляет коэффициент молча.

### Данные больше не теряются

- **Правки во вкладках ОТХОДЫ, ВЫБРОСЫ и СБРОСЫ не сохранялись вообще** —
  сборщик формы их не читал, хотя писалось «Сохранено ✓».
- Две площадки с длинным похожим адресом писали в одну папку — теперь к
  обрезанному имени добавляется отпечаток полного.
- «Освободить место» стирало корзину, куда программа обещала складывать
  удалённые площадки; теперь корзина чистится отдельной командой.
- Правка из другого окна или с другого компьютера больше не затирается молча.
- «Задание ИИ» делает копию данных перед заменой раздела и предупреждает,
  если записей стало меньше.
- Приём перестал удалять нечитаемые файлы (это был единственный экземпляр).
- Реквизиты организации пишутся атомарно; битый файл не блокирует площадки.

### Формы приведены к действующим бланкам

- **4-ОС**: правильный индекс формы (был «4-ООС»), одна таблица бланка,
  строки 01–10 вместо выдуманных 101–106, раздел платы за НВОС убран —
  в действующей форме его нет.
- **2-ТП (отходы)**: раздел III — 21 строка формы (11–31) вместо самодельной
  таблицы объектов; из названия убрано «транспортировании»; на титуле блок
  «кто предоставляет / срок / чем утверждена».
- **2-ТП (воздух)**: группы суммации (коды 6xxx) исключены — их массы уже
  учтены в веществах группы; вещества раскладываются по строкам 102–109
  по нормализованному коду и вдвое более полному справочнику.
- **Декларация**: строки ОКТМО после каждого КБК, детализация ПНГ и ТКО,
  итоговая часть 130–170 (вычет, авансы, доплата/возврат); на титуле —
  ФИО предпринимателя, а не «сокращённое наименование».
- **Отчёт ПЭК**: таблица 2.2 строится по источникам выбросов с графами г/с
  и т/год (данные лежали в базе и не использовались), раздел 3 разбит на
  объёмы и качество, титул — по бланку.
- **Отчётность III категории** честно сообщает, что отдельной федеральной
  формы нет и сведения идут в составе отчёта ПЭК.
- Коды веществ выводятся в отчёты и XML в официальном виде («0301»).

### Календарь

- «III категория» не распознавалась — с реальной базы пропадали 7 обязанностей.
- Добавлены внесение платы (1 марта), 4-ОС (25 января) и блок РОП.
- Сроки переносятся с выходного; ответственность указывается по виду формы.
- 2-ТП (водхоз) и проект НДС требуются только при сбросе в водный объект,
  а не при любой строке со средой «вода»; СЗЗ — по отдельному признаку.
- Появилась отметка о сдаче: она снимает «просрочено».

### Каталог ФККО

Обновлён с официальной страницы Росприроднадзора: **9146 позиций** вместо
7499 (+1707 кодов). Из двух каталогов — вашего и поставляемого — программа
берёт более свежий.

### Мелочи, которые мешали

Пакет к подаче больше не собирается при ошибках проверки и ведёт в нужное
ведомство (кадастр — в ГИС СПб, статформы — в Росстат, а не всех в ЛКПП);
готовый документ не перезаписывается молча; кнопка «Открыть» после генерации
работает; «Проверить всё» показывает сводку; кнопка «Взять» объясняет, почему
значение не записано, и понимает «12,3 т/год»; на вкладке «Общая» появились
поля программы ПЭК и затрат на ООС, которых требовала проверка; в «Сервисе»
видно, сколько места занимает база.

Тесты: 281 (добавлен tests/test_audit_fixes.py — 29 страховок).
install.bat НЕ нужен.

## 0.53.1 — 2026-08-05

Итог полной перепроверки v0.53.0 (адверсариальное ревью: 13 находок,
6 подтверждено скептиками и воспроизведением, все закрыты).

- **ВАЖНОЕ: исправлена шкала класса опасности отхода по K.** Границы были
  сдвинуты на порядок, и расчёт занижал класс на единицу (K=500 давал IV
  вместо III). Теперь строго по приложению № 1 к Критериям:
  10⁶ ≥ K > 10⁴ — I; 10⁴ ≥ K > 10³ — II; 10³ ≥ K > 10² — III;
  10² ≥ K > 10 — IV; K ≤ 10 — V (верхняя граница включается). Сверено с
  официальным сканом приказа с pravo.gov.ru двумя независимыми проверками.
  Если вы оформляли расчёт класса в v0.53.0 — перегенерируйте документ.
- **Реквизиты НПА обновлены**: приказ № 536 от 04.12.2014 утратил силу
  с 01.09.2025 — в расчётах, документах и подписях теперь действующий
  приказ Минприроды № 158 от 31.03.2025 (шкала K в нём не менялась).
- **Каталог ФККО не затирается**: загруженный вручную каталог автоподхват
  из «Форм» больше не перезаписывает при старте (origin=manual/forms);
  кнопка «Обновить каталог ФККО» теперь сначала берёт файл из «Форм»,
  сеть — запасной путь; из нескольких файлов ФККО берётся самый свежий;
  сообщение «кода нет» называет дату снимка каталога.
- **Проверка бланков по НПА**: .html-бланки из ЛКПП читаются (снимаем теги
  сами), скан без текстового слоя — предупреждение, а не ложное «это
  другой документ»; онлайн-проверка больше не «съедает» снимки штатного
  сторожа форм (watch check покажет изменение сам).
- **Табличка по отходам**: объём берётся и из пары «т + м³» справок-актов
  (как в «Сводной»), а не только из явной плотности; ошибка «перечень
  пуст» теперь видна и на вкладке ОТХОДЫ.
- **Уведомления о бланках**: изменения, замеченные проверкой при запуске,
  не пропадают — кнопка «Бланки документов» показывает их тоже; снимок
  реестра пишется атомарно (tmp+replace).
- Имена файлов без года — без хвоста «_»; строгий разбор класса опасности
  в выгрузке ФККО (одна цифра, не подстрока).

Тесты: 232 passed. install.bat НЕ нужен.

## 0.53.0 — 2026-07-31

Закрытие остатков ТЗ «Замечания 27/07/26»: полный каталог ФККО из папки
«Формы», раздел ООС, дозагрузка в каждом разделе, две оценки грунта,
проверка бланков по НПА, табличка отходов, документ расчёта класса
опасности и обвязка обмена с УПРЗА для тома НДВ.

- **Полный ФККО из «Форм»**: выгрузка `ФККО.xls` (7499 кодов) разбирается
  напрямую по ячейкам (коды-числа, римские классы), подхватывается сама —
  при запуске и при обновлении файла; каталог пользователя хранится в общей
  базе (`справочники/fkko.json`), а встроенный полный каталог идёт с
  программой — проверка кодов работает из коробки.
- **Раздел ООС** (ПП РФ № 87, п. 25) в «Разработке»: титул, содержание,
  все 10 пунктов, таблицы выбросов/сбросов/отходов из базы, типовые
  мероприятия по средам с воздействием, затраты из расчёта платы;
  чего не хватает — пометки «[требуется: …]» в тексте и тот же список
  в ответе кнопки.
- **Дозагрузка исходников в разделах**: блок «📥 Догрузить исходники»
  во вкладках ОРГАНИЗАЦИЯ, ОБЪЕКТ, ОТХОДЫ, ВЫБРОСЫ, СБРОСЫ — с категорией
  под раздел; после приёма данные вкладки перечитываются, отчёт остаётся.
- **Оценка грунта двумя путями** («Сервис»): категория почвы по
  СанПиН 1.2.3685-21/МУ 2.1.7.730-99 (Zc по фону + кратности ПДК/ОДК,
  рекомендация, куда грунт можно) и класс отхода по приказу № 536 —
  и заключение одним .docx.
- **Проверка бланков по НПА** («Сервис»): признаки формы в файле бланка,
  давность бланка против действующей редакции приказа, по кнопке — ещё и
  проверка страниц-источников в интернете (сторожок изменений).
- **Табличка по отходам** — по бланку пользователя «форма таблички по
  отходам.xls»: т/м³ по классам опасности с «ИТОГО» и «ВСЕГО», плотность
  из справок-актов, м³ не выдумывается.
- **Документ расчёта класса опасности** (пр. № 536): кнопка «Оформить
  расчёт (.docx)» в калькуляторе — таблица компонентов Ci/Wi/Ki, K, вывод,
  примечание про биотестирование для V класса.
- **Автопроверка форм при запуске**: новые/пропавшие бланки в «Формах»
  и свежий ФККО показываются плашкой на главном экране.
- **Том НДВ/НДС/СЗЗ + УПРЗА** (вкладка «УПРЗА»): круг обмена — выгрузка
  источников для «Эколога» → расчёт там → его Excel-выгрузки загружаются
  обратно → том .docx собирается с настоящими таблицами; без выгрузок
  источники берутся из инвентаризации, заглушка называется заглушкой.

Тесты: 225 passed.

Этап 8, последний по плану: выбор места хранения базы и сквозная проверка
всего пути — от загрузки документов до готовых форм.

- **Выбор хранилища** в «Сервис → Хранение»: «общая (OneDrive) — доступна со
  всех компьютеров» или «только на этом компьютере». При переключении данные
  переносятся в выбранную сторону: чего нет — копируется, при совпадении
  площадки побеждает более свежая, прежняя сохраняется рядом в папке-бэкапе,
  **исходная папка остаётся на месте**. Если OneDrive не найден, вариант
  «общая» недоступен и об этом прямо сказано.
- **Сквозной прогон** (`tests/test_e2e_full_cycle.py`): загрузка трёх
  документов → расхождение ИНН между выпиской и счётом → выбор значений →
  справки-акты → пересчёт движения → проверка ФККО и протоколов → генерация
  **всех девяти отчётных форм**, пяти документов разработки, сводной по
  периодам и пакета к подаче в ЛКПП. Всё на одном наборе данных, без сети.
- Исправлено: файл локальных настроек (`ui.json`) вычислялся один раз при
  запуске и не видел `ECODOC_HOME` — из-за этого тест однажды переключил режим
  хранения на рабочей машине. Теперь путь читается каждый раз, тесты пишут
  только во временные папки, а изоляция проверяется тестом.

Итог по плану перестройки (этапы 1–8): 190 тестов, 19 форм и документов,
вкладки ЗАГРУЗКА → ДАННЫЕ → ОРГАНИЗАЦИЯ → ОБЪЕКТ → ОТХОДЫ → ВЫБРОСЫ →
СБРОСЫ → Отчётность → Разработка.

## 0.51.0 — 2026-07-31

Этап 7: пять документов, которых не было вообще или которые были пустыми
каркасами. Порядок — от питающих базу к проектным, как договаривались.

- **Инвентаризация отходов** (новый документ): перечень отходов объекта,
  собранный из справок-актов, движения, паспортов и протоколов — с классом
  опасности, составом, видами обращения и приёмщиками. Отдельный лист «Чего
  не хватает»: где нет паспорта, состава, класса, протоколов.
- **Инвентаризация выбросов** (пр. МПР № 871, был пустой каркас): источники
  с веществами (г/с и т/год) и сводка по веществам; видно, из какого файла
  взят каждый источник.
- **ПНООЛР** (был пустой каркас): расчётная часть — норматив образования и
  лимит на размещение по каждому отходу, итоги. Описательные разделы честно
  перечислены как «пишется экологом» — программа их не выдумывает.
- **4-ООС** (нового документа не было): статистическая форма о текущих
  затратах на охрану окружающей среды — титул, раздел 1 по направлениям
  затрат, раздел 2 с платой за НВОС (берётся из расчёта декларации).
- **ТУ** (был пустой каркас): письмо-запрос технических условий на приём
  отходов с таблицей отходов, реквизитами сторон и перечнем приложений.
- Паспорта отходов теперь тоже видны в списке документов и генерируются
  кнопкой (раньше — только командой).

Итого в контуре «Разработка» семь работающих генераторов (было три),
в «Отчётности» — девять форм (было восемь).
ИЭИ и ОЦЕНКА не делаются — по вашему решению от 31.07.

## 0.50.0 — 2026-07-31

Этап 6: программа знает ваши **бланки документов** и заполняет их, сохраняя
формулы Excel.

- **Реестр бланков** (`ecodoc/core/forms_registry.py`): смотрит папку
  `OneDrive\Формы`, сопоставляет 20 позиций (отчётность и разработка) с
  документами программы и показывает, где образец есть, а где нет. На вашей
  папке: **8 бланков из 20** (2-ТП отходы, журнал 1028, декларация, отчёт ПЭК,
  ПОО, программа ПЭК, НДВ, перечень для контроля).
- **Поиск недостающих на диске**: кнопка «Поискать недостающие» ищет подходящие
  файлы в `D:\S` по названию и показывает, что можно положить в папку «Формы».
- **Отслеживание новых бланков**: при проверке сравнивается с прошлым разом —
  видно, что появилось и что пропало (снимок в `реестр-форм.json`).
- **Заполнение бланка вместо рисования** (`ecodoc/render/template_xlsx.py` +
  `reports/waste_movement/template.py`): журнал 1028 теперь заполняется в
  **вашем файле** — графы находятся по подписям (бланки у всех смещены
  по-разному), **формулы не затираются**, расчётные графы Excel посчитает сам.
  Нет бланка — форма строится кодом, как раньше.
- Путь к папке форм больше не зашит под один компьютер: `ECODOC_FORMS`, иначе
  `OneDrive\Формы` текущего пользователя, иначе `D:\S\Формы`.
- В «Сервис → Хранение» — раздел «Бланки документов» со всем перечисленным.

⚠ Соответствие бланка действующим нормам программа не подтверждает: она
показывает, что есть, и заполняет данными. Проверка бланка на актуальность —
за вами (сторож НПА в «Сервис → Сторож форм» подсказывает об изменениях).

## 0.49.0 — 2026-07-31

Этап 5: отдельные вкладки **ОТХОДЫ**, **ВЫБРОСЫ**, **СБРОСЫ** и автопроверка
кодов отходов по каталогу ФККО.

- **Вкладка ОТХОДЫ**: справки-акты, движение, паспорта, разбивка по
  месяцам/кварталам/году и сводная таблица — всё в одном месте, с ручным
  редактированием.
- **Проверка ФККО по каталогу** (`ecodoc/core/fkko.py`): для каждого кода —
  структура (11 цифр, класс опасности 1–5), наличие в каталоге, расхождение
  наименования и класса опасности с каталогом. Проверка запускается при
  открытии вкладки.
- **Каталог ФККО**: кнопка «Обновить каталог» — попытка скачать из открытых
  источников либо **загрузка файла**, скачанного вручную (xlsx/csv/json).
  Машиночитаемого открытого ФККО в сети нет (Росприроднадзор публикует каталог
  страницей), поэтому файл — основной путь. Из коробки работает встроенный
  минимум из справочника частых отходов; он помечен как **частичный** —
  отсутствие кода в нём не считается ошибкой, программа честно пишет
  «загрузите полный ФККО, чтобы проверить».
- **Вкладка ВЫБРОСЫ**: вещества с массами и разбивкой по периодам.
  **Вкладка СБРОСЫ**: вещества, забор воды, водоотведение — ручной ввод.
- Вкладка ДАННЫЕ стала общей: отчётный год и указатели, где что заполнять.

## 0.48.0 — 2026-07-31

Этап 4: отдельные вкладки **ОРГАНИЗАЦИЯ** и **ОБЪЕКТ**, видно с чем работаем,
смена объекта — только с подтверждением.

- **Вкладка ОРГАНИЗАЦИЯ**: полные реквизиты и кнопка **«Сверить с ЕГРЮЛ по
  ИНН»** — таблица «у нас / в ЕГРЮЛ», расхождения помечены, любое поле (или все
  сразу) можно взять из ЕГРЮЛ. Если ИНН не заполнен — программа его спросит.
- **Вкладка ОБЪЕКТ**: адрес, код НВОС, кнопка проверки — формат кода,
  категория объекта по последней букве и определение **ОКТМО/ОКАТО по адресу**
  с записью в реквизиты. Удаление некорректных объектов — здесь же.
- **Единое правило кода НВОС** (`ecodoc/core/nvos.py`): формат
  **40-0278-013459-П**. Раньше проверка была в трёх местах и по-разному — одна
  отбрасывала суффикс «Б», другая пропускала латиницу. Теперь латинские
  двойники (P, T, O, L, B) приводятся к кириллице, а на некорректный код
  программа объясняет, что не так («это кадастровый номер», «нужен формат …»).
- **Постоянная плашка вверху**: организация с ИНН, объект с адресом и кодом
  НВОС, отметка о несохранённых правках.
- **Смена объекта — с подтверждением**: показывается, с какого объекта на какой
  переходим, и предупреждение, если есть несохранённые правки. Данные всех
  вкладок перечитываются под новый объект.
- Сохранение собирает поля со всех вкладок (ДАННЫЕ, ОРГАНИЗАЦИЯ, ОБЪЕКТ).

## 0.47.0 — 2026-07-31

Этап 3: **найденные данные на выбор** — видно, что нашлось в каждом документе,
где расхождения, и что взять в базу.

- **Кандидаты вместо молчаливого заполнения.** Каждое найденное значение
  запоминается вместе с источником: файл, лист, цитата, кто нашёл (ИИ или
  обычный разбор). Хранится в `candidates.json` рядом с данными площадки.
- **Кросс-сверка между документами.** Одно значение из нескольких файлов —
  «подтверждено N документами»; разные значения — расхождение, программа
  показывает оба варианта со ссылкой на лист каждого и спрашивает, что взять.
  Отдельно распознаётся путаница **килограммов и тонн** (12 кг и 0,012 т —
  одно и то же; 12 против 0,012 без единиц — предупреждение).
- **Ключевые реквизиты (ИНН, КПП, ОГРН, ОКТМО, год, код объекта) не пишутся
  молча** даже при единственном варианте — только с подтверждением.
  Бесспорные значения в пустые поля применяются автоматически.
- **Конкретные вопросы вместо «не хватает»**: «Введите ИНН: 10 цифр у
  юридического лица, 12 у ИП», «Введите ОКТМО — 8 или 11 цифр». Если по полю
  уже есть найденный вариант, вопрос не задаётся — сначала выбор.
- **Проверка протоколов по классу опасности**: для IV класса нужен КХА или
  санитарно-химический протокол, для V — дополнительно
  токсикология/биотестирование на 2 форм-факторах; **отсутствие биотеста
  прямо называется** в отчёте загрузки и во вкладке «Данные».
- Во вкладке «Данные» новый блок «Найденные данные»: таблица «поле → вариант →
  откуда (файл, лист) → взять», ввод своего значения, список принятого.

## 0.46.0 — 2026-07-31

Этап 2: вкладка **ЗАГРУЗКА** и прослеживаемость — видно, **с какого листа**
взято каждое значение, и сам лист можно открыть.

- **Скан листа-источника.** При разборе сохраняется картинка **только тех
  листов, где что-то нашлось** (JPEG, оттенки серого, ~60–90 КБ на лист против
  мегабайтов у исходника). Исходные файлы по-прежнему не хранятся.
  Ограничители: не более 30 листов на документ и 300 МБ на площадку.
- **Отдельная страница просмотра** `/source`: лист крупно, имя файла, способ
  разбора («текстовый PDF» / «распознано OCR»), что именно с этого листа взято,
  переходы к соседним листам и печать.
- **Постраничный разбор.** Запросы к модели теперь режутся **по страницам**, а
  не по 14 000 символов подряд — поэтому лист известен всегда, даже если модель
  не вернула цитату. Цитата ищется по листам (точное совпадение помечается).
- **Листы для всех форматов**: у Excel «лист» — это лист книги («лист
  «Отходы 2025»»), у Word/RTF/XML — фрагмент («фрагмент 3 из 7»), у PDF и
  сканов — страница.
- Прослеживаемость работает и **без ИИ**: реквизиты и коды НВОС, найденные
  обычным разбором, тоже отмечают лист-источник.
- **Таблица разобранных документов** под загрузкой: файл, способ разбора,
  сколько значений найдено и ссылки «📄 N» на листы-источники.
- **Пауза и продолжение** загрузки (раньше был только «Стоп»).
- **Загрузка по ссылке**: поле «или ссылка на файл» — программа скачивает
  документ по http/https (лимит 300 МБ) и разбирает как обычный.
- Паспорта разобранных документов — в отдельном файле `sources.json`
  (в `context.json` не лезут: он и так большой и переписывается целиком).

## 0.45.0 — 2026-07-30

Этап 1 перестройки по схеме от 27.07.26: все модели из ТЗ, автовыбор лучшей
на текущий момент и окно доп. задания ИИ в каждом разделе.

- **Реестр моделей** (`ecodoc/ai/registry.py`): все провайдеры из ТЗ —
  Mistral, Gemini, Cohere, Groq, Cerebras, OpenRouter, DeepSeek, OpenAI,
  Anthropic, Moonshot, GigaChat, YandexGPT, локальные Ollama и LM Studio.
  У каждой модели тариф (бесплатно / локально / платно), известные лимиты и
  замер на реальной задаче (качество извлечения и время на документ).
- **Проверка моделей при запуске** (`ecodoc/ai/health.py`): программа опрашивает
  все модели, у которых есть ключ, и переключается на **лучшую рабочую** по
  порядку из ТЗ — *сначала большие бесплатные, потом локальные, и только потом
  платный DeepSeek*. Результат кэшируется на 6 часов, проверка идёт в фоне и
  не задерживает открытие окна. Команда `ecodoc ai-health` (`--cached` — без опроса).
- **Вкладка «Сервис» → «Модели ИИ»**: таблица состояния (работает / лимит /
  нет ключа / регион), тариф, время ответа, лимиты, подсветка выбранной модели
  и кнопка «Проверить все модели».
- Причины отказа теперь называются точно: **блокировка по региону** (Gemini
  отвечает «User location is not supported», Groq и Cerebras — Cloudflare 1010)
  больше не выглядит как «неверный ключ»; отдельно распознаются исчерпанная
  квота и нехватка средств на ключе.
- **Окно «🤖 Задание ИИ» в каждом разделе** (`ecodoc/ai/task.py`): пользователь
  пишет задание своими словами («пересчитай массы из кг в тонны», «сведи
  одинаковые отходы»), модель получает **только данные этого раздела**.
  Ответ показывается; изменения вносятся в базу отдельной кнопкой
  «Применить к базе» и проходят штатный разбор типов.
- Все ключи из ТЗ записаны в общую базу (`ai_keys.json` в OneDrive) —
  работают на любом компьютере; для каждого провайдера выбран проверенно
  рабочий вариант ключа.
- `serialize.from_dict` — единая точка приведения сырых данных к типам модели
  (использует и загрузка файла, и форма, и ответ ИИ).

Проверено замером (30.07.2026, задача — извлечение из справки-акта, 19 полей):
Mistral Small 19/19 ~9 с (выбрана), Cohere 19/19, OpenRouter :free 19/19 но
медленно, DeepSeek 19/19 (платный), Ollama qwen2.5 локально; Gemini, Groq,
Cerebras недоступны из РФ без VPN, OpenAI — исчерпана квота, Anthropic — нет
средств, Moonshot — ключ пустой.

## 0.44.0 — 2026-07-18

ИИ по умолчанию — бесплатный **Cohere**; ключ вводится один раз на все компьютеры.

- **Новый провайдер Cohere** (Chat API v2), модели `command-a-03-2025`
  (по умолчанию) и `command-r7b-12-2024` (быстрая). Ключ бесплатный, без
  привязки карты: dashboard.cohere.com/api-keys. Проверено на реальной
  справке-акте: оба акта извлечены полностью (даты, массы, объёмы, вид
  обращения, перевозчик, приёмщик, лицензия) — ~14 с на документ против
  минут у локальной модели.
- **Автонастройка при первом запуске**: есть ключ бесплатного облака —
  работаем через него, нет — через локальную Ollama. Старые конфиги с
  Ollama один раз переводятся на бесплатное облако (локальная модель
  остаётся последней в цепочке — работает без интернета).
- **Ключ можно сохранить в общую базу** (галочка «на всех моих
  компьютерах»): вводится один раз и подхватывается на ноуте и десктопе.
  Локальный ключ машины (`~/.ecodoc/keys.json`) важнее общего.
- **Лимит бесплатного ключа обрабатывается**: при HTTP 429 (у Cohere
  20 запросов/мин) — пауза и повтор того же провайдера, а не уход на
  запасной; параллелизм для Cohere снижен до 4 запросов.
- **Неверный ключ (401/403) выключает провайдера на час** — битый ключ
  больше не тормозит каждый документ.
- Добавлен провайдер Cerebras; у OpenRouter/Mistral/Gemini дефолтные модели
  обновлены на живые бесплатные (`gpt-oss-20b:free`, `mistral-small-latest`,
  `gemini-flash-latest`) — прежние сняты с бесплатного тарифа.
- Настройки ИИ читают `ECODOC_HOME` динамически; тесты изолированы от
  реального конфига и ключей машины.
- **Паспорта отходов** (Приказ №1026) — форма приведена к принятым образцам
  (гриф «УТВЕРЖДАЮ», «Сведения об отходах», «Сведения о лице, которое
  образовало отходы»), состав подтягивается из загруженных паспортов/КХА;
  исправлено падение при отходе без известного состава.

## 0.43.0 — 2026-07-18

База — только данные; готовые документы — в папку результатов, не в OneDrive.

- **Готовые документы больше не хранятся в базе**: генерация, «Пакет к
  подаче», сводная по отходам, документы разработки и выгрузка УПРЗА
  сохраняются в **папку результатов** на этом компьютере (по умолчанию
  `Загрузки\ЭКО.DOC\<организация>\<площадка>`). Путь настраивается в
  «Сервис → Хранение» (или env `ECODOC_RESULTS`) и показывается после
  каждой генерации.
- **Кнопка «Освободить место в базе»** (Сервис → Хранение): удаляет из базы
  сгенерированные документы и исходники старше 7 дней (свежие могут ждать
  анализа); отчёты приёма и данные не трогаются, реестр подчищается, чтобы
  повторная загрузка тех же файлов работала.
- Перенос базы в OneDrive больше **не копирует папки out** (готовые формы —
  не данные).
- В «Сервис → Хранение» видно, где база и где результаты.

## 0.42.0 — 2026-07-18

Общая база данных в OneDrive — одни и те же организации/площадки/данные
с любого компьютера (ноут и десктоп).

- **База теперь живёт в `OneDrive\ЭКО.DOC`** (если OneDrive есть на машине).
  При первом запуске локальная база `%USERPROFILE%\ЭКО.DOC` **автоматически
  вливается** в общую: недостающие организации/площадки переносятся целиком;
  если площадка есть и там и там — побеждает более свежая (последняя работа),
  проигравшая версия сохраняется рядом в папке-бэкапе. Локальная папка после
  переноса переименовывается (данные не удаляются); лог переноса пишется
  в общую базу (`перенос-<компьютер>-<дата>.txt`).
- В шапке GUI виден путь к базе и пометка «☁ общая (OneDrive)» с напоминанием:
  **не работать с одной площадкой на двух компьютерах одновременно**
  (OneDrive при одновременной правке создаёт конфликтующие копии).
- Переопределение осталось: переменная `ECODOC_WORKSPACE` — использовать
  указанную папку (например, чисто локальную базу без OneDrive).
- Настройки ИИ (`~/.ecodoc`: провайдер, ключи) остаются локальными на каждой
  машине — на ноуте может быть Ollama, на десктопе облако.
- Тесты изолированы от реальной базы (conftest: ECODOC_WORKSPACE → tmp).

## 0.41.1 — 2026-07-17

Фикс «не загрузилось 79 файлов (сетевые сбои)» из живого теста.

- **Таймаут загрузки партии 2 мин → 10 мин**: архивы распаковываются сервером
  прямо при загрузке (7-Zip, до 300 МБ) — на большом архиве клиент обрывал
  запрос по таймауту и после 3 попыток хоронил всю партию до 60 файлов как
  «сетевой сбой», хотя сервер продолжал работать.
- В предупреждении о несохранённых файлах теперь видна **настоящая причина**
  (текст последней ошибки), а не обезличенные «сетевые сбои»; добавлено
  пояснение, что повторный приём тех же файлов не создаст дублей (sha1-реестр).
- Список «Пропущено при загрузке» без дублей и сгруппирован по причинам;
  подписи **.sig** — одной строкой с пояснением (данных в них нет).
- Файлы, которые браузер не смог прочитать при кодировании, попадают в
  «Пропущено» с причиной (раньше исчезали молча).

## 0.41.0 — 2026-07-17

Модули по средам + паспорта отходов + сводная «как Справки-2025» (по предложению
пользователя о разделении программы на модули; одна база — модули поверх неё).

- **Отчётность разделена на модули**: Общая (плата и ПЭК) / Отходы / Воздух /
  Вода. В каждом модуле: **своя загрузка документов** (с правильной категорией),
  свои формы и чек-лист **«чего не хватает»** (обновляется после загрузки).
- **Разработка разделена на модули**: Отходы (ПНООЛР, класс опасности, ТУ) /
  Воздух (НМУ, инвентаризация) / Вода / Общее (ПЛАРН, ДВОС, программа ПЭК);
  в модулях — загрузка исходников (паспорта/ООС/НДВ).
- **Паспорта отходов**: новая категория загрузки — ИИ извлекает ФККО,
  наименование, класс и **состав** (компоненты с %) → справочник
  «Паспорта отходов» в Данных (раздел 1. Отходы).
- **Источники выбросов**: из ООС/НДВ/инвентаризации ИИ извлекает источники
  (№, наименование, вещества г/с и т/год) → extra.emission_sources.
- **Сводная таблица по отходам** из справок-актов — по форме рабочей таблицы
  эколога («Справки-2025» у ЛСС): строки — отходы (наименование, ФККО с
  группировкой, класс, плотность), колонки — **месяцы / кварталы / год**
  парами «т | м³», хвост — вид обращения, перевозчик, приёмщик. Кнопка в
  модуле Отходы + команда `ecodoc waste-summary`. Объём считается из
  плотности, если в акте не указан.
- Справки-акты: добавлены поля **объём м³, плотность, лицензия перевозчика,
  лицензия полигона** (и в таблице, и в ИИ-извлечении).
- Категория «прочее» на Приёме: документ кладётся в базу целиком (extras),
  не трогая реквизиты/акты/вещества.

## 0.40.0 — 2026-07-17

По разбору живого теста (21 файл, Ollama): не терять файлы при сбое ИИ,
не спамить отчёт, дочистить мусор старых версий.

- **Сбой ИИ ≠ потеря файлов**: исходники, которые ИИ не проанализировал
  (провайдер упал/не настроен), больше **не удаляются** из приёма — остаются
  с пометкой «нажмите Анализ ещё раз». Раньше файл удалялся, даже если ИИ
  до него не добрался.
- **Локальный Ollama не «хоронится» на 5 минут** из-за таймаута на большом
  документе: остывание — только при явном отказе соединения, и короткое (60 с).
  Для облачных провайдеров поведение прежнее.
- **Конфликты сгруппированы**: одинаковый конфликт из многих договоров
  («name: ИП Миних — в 8 документах») — одна строка вместо восьми.
- **КПП у ИП вычищается автоматически** при загрузке базы — мусор из счетов
  контрагентов, сохранённый старыми версиями, исчезает сам (у ИП КПП не бывает).
- Сводка распознавания помечает некорректные коды объектов НВОС значком ⚠
  с подсказкой удалить их кнопкой в «Данные → Объекты НВОС».
- Блоки «Разбивка по периодам» теперь видны всегда: если данных нет, вместо
  пустоты — подсказка, откуда возьмётся разбивка (даты справок-актов /
  годовые массы веществ). Поле «Год» больше не показывает 0.

## 0.39.0 — 2026-07-17

Раздельная загрузка документов по категориям + чистка мусорных объектов НВОС.

- **«Что загружаем» на вкладке Приём**: авто (все данные) / реквизиты
  (ЕГРЮЛ, карточка, объекты НВОС) / справки-акты на отходы / воздух (ООС, НДВ) /
  вода (НДС, водхоз). Категория ограничивает, какие поля контекста может
  трогать разбор: счета-фактуры в партии «справки» больше не подменяют
  реквизиты организации чужими КПП/ОКПО, а проекты-шаблоны — объекты НВОС.
- **ИИ извлекает вещества**: новые блоки `pollutants_air` (ПДВ/ВСВ из ООС и
  НДВ) и `pollutants_water` (НДС/ВСС) — заполняют разделы «Воздух» и «Вода»
  только пустыми значениями, конфликты показываются в отчёте.
- **Кнопка «Удалить некорректные»** в разделе «Объекты НВОС» вкладки Данные:
  одним нажатием убирает сохранённые ранее мусорные коды (не формат НВОС
  `XX-XXXX-XXXXXX-Б` и не кадастровый) — валидные объекты не трогаются.
- Ошибки разбора в отчёте приёма группируются («N файлов» вместо повтора
  одной и той же строки на каждый файл).

## 0.38.0 — 2026-07-16

Разбивка по периодам перенесена внутрь разделов (по фидбэку живого теста).

- Из шапки «Данных» убран выбор квартала — остался только отчётный год.
  Переключатель **месяц/квартал/год** теперь внутри разделов «Отходы»,
  «Воздух», «Вода»: отходы разбиваются по фактическим датам актов,
  вещества — равными долями.
- Раскрывающиеся блоки разбивки в каждом разделе (сворачиваются независимо).

## 0.37.0 — 2026-07-15

Чистка мусора распознавания + «Данные» по средам (фидбэк живого теста на 21 файле).

- Телефоны и реквизиты счетов больше не распознаются как коды ФККО
  (проверка подгрупп блоков 7/8/9 + анализ контекста перед числом);
  встреченные коды ФККО теперь идут в подсказки, а не в движение отходов.
- ИП больше не получает КПП из счетов контрагентов (и в ИИ-, и в regex-пути).
- Объекты НВОС из ИИ принимаются только с валидным кодом НВОС или кадастровым.
- «Данные» перегруппированы: 1. Отходы (акты + движение) / 2. Воздух / 3. Вода;
  нулевые строки движения спрятаны в свёрнутый блок с кнопкой удаления.
- Пофайловый приём чанками не теряет файлы следующих чанков (purge только
  проанализированных); ISO-даты актов («2025-02-01») разбираются корректно.

## 0.36.1 — 2026-07-14

Процесс релиза: новая версия = новый номер ВЕЗДЕ.

- `release.ps1` теперь при каждом релизе создаёт **распакованную рабочую
  папку `releases\EKO.DOC-v<номер>`** (готовую к запуску: код из zip +
  перенесённая `.venv` из предыдущей установки — переустановка не нужна),
  ставит **git-тег версии** и пушит его (версии видны на GitHub в Releases/Tags).
- Правило: код больше не подкладывается в папку со старым номером — каждая
  версия живёт в папке со своим номером.

## 0.36.0 — 2026-07-13

Закрыты последние отложенные пункты аудита.

- **Кст — стимулирующий коэффициент за размещение отходов** (ст. 16.3 ФЗ-7)
  теперь применяется в расчёте платы: поле «Кст» у позиции движения (пусто =
  1; 0,3 — отходы IV–V кл. от утилизации/обезвреживания в пределах лимита;
  0 — утилизация в собственном производстве). Колонка в таблице «Отходы»,
  значение видно в листах деталей декларации (Кдоп).
- **Защита от lost-update**: пока идёт приём/анализ площадки (минуты OCR/ИИ),
  сохранение «Данных» по ней блокируется с понятным сообщением — правки
  больше не могут перезаписать друг друга; двойной запуск приёма по одной
  площадке также блокируется. Между чанками анализа правки подхватываются.
- **Код территориального органа РПН** в XML (RPN_TO/RPN_CODE) — из данных
  площадки (`extra.rpn_to`), жёсткая «1» была неверна для других регионов.

## 0.35.0 — 2026-07-13

Аудит, часть 2: разобраны все 40 находок пяти ревизоров (критикалы, формы,
надёжность, GUI, оптимизации).

- **КРИТИКАЛ (приём): чанкованный анализ больше не теряет файлы.** Чистка
  исходников после чанка удаляла ВЕСЬ каталог — файлы следующих чанков (>40
  файлов) пропадали. Теперь удаляются только обработанные в этом вызове +
  их записи реестра.
- **КРИТИКАЛ (периоды): ISO-даты актов** («2025-02-01») парсились как 2001
  год — акт молча выпадал из отчётного года. Поддержаны оба формата; в
  таблице актов появилась колонка «Дата».
- **КРИТИКАЛ (2-ТП XML): семантика TP2_TR_ISPOTX/TP2_TR_SOTX** — это
  «передано на утилизацию/обезвреживание», а не собственные операции;
  графа 1 и баланс учитывают накопление на начало года.
- **Журнал №1028 Прил.2**: «Размещено всего» = «хранение»+«захоронение»
  (сходимость 12=13+14); Прил.3 заполняет «для обработки».
- **Слияние данных**: ФККО нормализуется («4 71 101…» == «47110101521» — отход
  не двоится), позиции без кода сливаются по имени, получатели обновляются
  из актов с сохранением ручных дополнений; дедуп актов устойчив к формату
  числа/даты. Вид обращения «обработка/сортировка» — отдельный поток.
- **Надёжность**: атомарная запись context.json; десятичная запятая («1,5»)
  не валит сохранение; одноимённые файлы из разных подпапок не затираются;
  Word COM из потоков с CoInitialize; справочники data/*.json перечитываются
  по mtime (правка ставок без перезапуска).
- **GUI**: ошибка загрузки площадки больше не оставляет данные предыдущей
  (риск записи в чужой context.json); замечания валидации показываются и
  при ошибке генерации; свой спиннер на «Разработке»; автоподстановка
  справочника отходов в таблице актов; удалён осиротевший api_compare.
- **Оптимизации**: кэш OAuth-токена GigaChat (25 мин), «остывание» упавшего
  провайдера ИИ на 5 мин (не долбим таймауты на каждом файле), единое
  определение ТКО для всех форм.
- Часть 1 аудита (тем же днём): **пересчёт движения из актов не стирает
  ручные поля** («размещено», остатки — слияние `_merge_flows` вместо
  перезаписи); вывод .docx на вкладке «Разработка» шёл в скрытый блок; после
  «Сохранить» данные перечитываются; кэш списка моделей Ollama; замок реестра
  приёма (многопоточный сервер); оффлайн-ОКТМО перечитывается по mtime;
  удалён мёртвый код снимков периода.

## 0.34.0 — 2026-07-11

Отчётность по периодам + приём без хранения исходников.

- **Периоды**: акты отходов фильтруются по отчётному году (и кварталу, если
  задан) — учитываются только акты этого периода по их дате; акты без даты
  включаются. Год обязателен: если не задан, приём выдаёт предупреждение.
  В отчёте приёма — разбивка массы **по кварталам** и всего за год
  (`period_breakdown`); движение считается за выбранный период.
- **Приём без копирования исходников**: после извлечения данных исходные
  файлы НЕ хранятся в папке программы — данные уже в базе `context.json`.
  Реестр и сами файлы удаляются (остаётся только отчёт приёма); повторная
  загрузка тех же файлов переизвлекается без дублирования (акты дедуплицируются).

## 0.33.0 — 2026-07-11

Перестройка интерфейса по фидбэку + устойчивость ИИ и фильтр шума ФККО.

- **Интерфейс**: вкладка «Генерация» → **«Отчётность»** с группировкой форм по
  средам (Отходы / Воздух / Вода / Плата и ПЭК); «Разработка» вынесена в
  **отдельную вкладку**; «Рассеивание» → вкладка **«УПРЗА»**; кнопки «Запомнить
  период» и «Сравнить с прошлым» убраны.
- **ИИ-фолбэк устойчивее**: если у провайдера в цепочке не задана модель
  (частая причина `Ollama HTTP 400 "model is required"`), он пропускается; для
  Ollama модель подбирается автоматически из установленных.
- **Фильтр шума ФККО**: 11-значные телефоны/реквизиты (ИНН/ОГРН/БИК/счета)
  больше не попадают в позиции отходов — проверка контекста + структуры блоков
  ФККО (7/8/9 имеют ограниченный набор подгрупп, «79…»/«89…» отсекаются).

## 0.32.0 — 2026-07-10

Кнопка «Пакет к подаче» в интерфейсе (контур подачи в ЛКПП).

- В GUI «Генерация» у отчётных форм — кнопка **«Пакет к подаче»**: собирает
  папку `out/<ИНН>_<форма>_<год>/` (XML + печать + МЧД + `ЧЕКЛИСТ.md`),
  показывает результат preflight и открывает папку. Endpoint `api_submit`.

## 0.31.0 — 2026-07-10

ИИ-анализатор извлекает справки-акты на отходы (первичный ввод).

- `analyze_docs` теперь кладёт извлечённые `disposal_acts` в `ctx.waste_acts`
  как справки-акты (наименование/ФККО/класс/масса/объём/вид обращения/
  перевозчик/приёмщик/лицензия/дата) — с дедупом по (ФККО, дата, получатель,
  масса), чтобы повторный анализ не двоил.
- В конце анализа акты сворачиваются в движение (`aggregate_acts`) + список
  получателей. Промпт дополнен полями `carrier`/`volume_m3`/`hazard_class`.
  Весь путь «загрузил справки → ИИ извлёк акты → движение → формы» работает.

## 0.30.0 — 2026-07-10

Агрегация актов — передача разнесена по видам обращения.

- `WasteFlow` +`transferred_util`/`transferred_neutral` (было storage/burial);
  агрегация актов раскладывает передачу по операции: утилизация → util,
  обезвреживание → neutral, хранение → storage, размещение/захоронение → burial.
- Формы получают точную разбивку: журнал №1028 Приложение 3 (графы «для
  утилизации/обезвреживания/хранения/захоронения») и 2-ТП (отходы) графы
  17/19/21/23 заполняются по видам передачи, а не всё в захоронение.

## 0.29.0 — 2026-07-10

Модель данных под реальный процесс: отходы вводятся справками-актами,
из них считаются все отходные формы; по воздуху — источник НДВ/ООС.

- **Справки-акты на отходы — первичный ввод** (`WasteAct`): наименование,
  ФККО, класс, масса, объём/плотность, **вид обращения** (утилизация/
  обезвреживание/размещение/хранение), **перевозчик**, **приёмщик/полигон**,
  дата. Соответствует реальным справкам (сверено с `Справки-2025.xls`).
- **Движение считается из актов** (`ecodoc/core/waste_agg.py`): агрегация по
  ФККО → образовано/передано (в т.ч. на захоронение/хранение) + список
  получателей для Приложения 3 журнала №1028 / кадастра / формы III кат.
  Акты первичны: заданы акты → движение пересчитывается из них; актов нет —
  остаётся заполненное вручную. Все отходные формы (1028, 2-ТП отходы,
  кадастр, раздел отходов декларации) берут данные из одного места.
- **По воздуху** у вещества добавлен признак источника (`Pollutant.source` —
  «НДВ»/«ООС»); в форме данных подсказка «массы берутся из проекта НДВ или
  раздела ООС».
- GUI «Данные»: таблица **«Справки-акты на отходы»** (первичный ввод) + блок
  движения (заполняется из актов) + подсказка по воздуху. `serialize`
  разбирает `waste_acts` и авто-считает движение при загрузке.

## 0.28.0 — 2026-07-10

Точная сверка по РЕАЛЬНЫМ принятым отчётам клиента из ЛКПП (D:\S\Формы
отчётности\МИНИХ): 2-ТП отходы на 29 граф и декларация на официальные коды строк.

- **2-ТП (отходы) — Раздел I на 29 граф** (Приказ Росстата № 614), сверено с
  принятым отчётом №13796482: наличие/образование/поступление (всего/из др.
  субъектов/импорт)/с собств. объектов/после обработки/обработано/утилизировано
  (рециклинг/после обработки)/обезврежено/**передача ТКО региональному
  оператору (графа 14)**/передача другим (обработка/утилизация/обезвреживание/
  хранение/захоронение, всего и в др. субъекты)/размещение/наличие на конец.
  ТКО (ФККО «7 3…») уходит именно в графу 14 (как в реальном отчёте).
- **Декларация НВОС — стр.2 на официальные коды строк 010–120** (Приложение 2
  к Приказу № 1043), сверено с реальной сданной декларацией: 010 ОКТМО; 020 =
  021+022+023+024+025; 040/060/080/100/120 с разбивкой по корзинам (041 в
  пределах НДВ/ТН, 042 в пределах ВРВ, 043 сверх и т.д.); КБК на 030/050/070/
  090/110. Суммы берутся из `calc.by_section` + разбивка по band.
- Подтверждено реальными данными: ОКТМО Янино **41612155** — верный (опасение
  «ОКАТО» из аудита снято); реквизиты ИП Миних (ОГРНИП 307784705100221,
  ОКПО 0114401551) — для карточки.

## 0.27.0 — 2026-07-10

Контур подачи в ЛКПП РПН + сверка XML 2-ТП (отходы) с тем, что реально
принимает кабинет (по итогам разведки: формат «Копии» = DATA_PACKET_NI).

- **Сверка XML под реального плательщика (в т.ч. ИП):**
  - `FINDIVID=true` для ИП (по 12-значному ИНН), КПП пустой, должность
    подписанта — «Индивидуальный предприниматель» (было «Генеральный
    директор»); `Organization.is_individual` / `official_title`.
  - `NOT_REG_OBJ` больше не захардкожен `true`: при указанном коде объекта
    НВОС (напр. `41-0247-000123-П`) выводится `REG_NUMBER` и `NOT_REG_OBJ=false`
    — устраняет отказ «объект не стоит на учёте».
  - `TP2_TR_DISP` = передано **на захоронение** (было ошибочно «всего
    передано» = `transferred`); в печати графа «для захоронения» — тоже.
  - **Preflight-валидация** перед подачей: обязательные ОГРН/ОГРНИП (с
    контрольной суммой), формат ОКТМО (8/11 цифр, не ОКАТО), наличие кода
    объекта НВОС, проверка ИНН.
- **Команда `ecodoc submit <форма>`** и модуль `ecodoc/submit`: собирает
  папку пакета `out/<ИНН>_<форма>_<год>/` — XML (DATA_PACKET_NI) + печать +
  файлы МЧД + **`ЧЕКЛИСТ.md`** (результат preflight, пошаговая инструкция
  загрузки в ЛКПП «Новый отчёт → Импорт XML → подписать УКЭП», памятка по
  МЧД с проверкой кода полномочия `RPNDZ_REPORT`).
- **Автоотправку по API РПН НЕ реализуем** намеренно: требует индивидуальной
  заявки на OD@rpn.gov.ru под каждый ОГРН, закрытой спецификации endpoints и
  подписи УКЭП+МЧД через КриптоПро — вне оффлайн-приложения. ЭКО.DOC —
  «генератор пакета», подача и подпись остаются за пользователем.

## 0.26.0 — 2026-07-10

Этапы 5–6: 2-ТП (водхоз) под Приказ № 445 и отчётность МСП/III кат.

- **2-ТП (водхоз)** — привязка к действующей форме: ОКУД **0609060**, Приказ
  Росстата **№ 445 от 02.10.2024**, адресат — территориальное **Бассейновое
  водное управление** (Модуль респондента / ГИС ЦП «Вода»), не Росстат.
  В водоотведение добавлены **коды и массы сброшенных ЗВ** по выпускам
  (`discharge[].pollutants`, Прил. 5 к № 445); оборотное и повторно-
  последовательное водоснабжение разделены; в «Сводку» вынесены форма/срок/
  адресат.
- **Отчётность МСП / III кат.** — добавлен лист **«Общие сведения»** с
  привязкой к объекту НВОС (код, категория, адрес, ОКТМО) и полными
  реквизитами; таблица движения расширена до **полного баланса масс**
  (наличие на начало / образовано / поступило / утилизировано / обезврежено /
  передано / размещено / наличие на конец) с проверкой баланса. XML дополнен
  объектом НВОС и остатками на начало/конец.

## 0.25.0 — 2026-07-10

Этап 4: 2-ТП (воздух) — переписана под структуру формы (Приказ № 661).

- **Убраны неверные графы** «в пределах норматива/лимита/сверх» (они относятся
  к декларации о плате, а не к статформе).
- **Раздел 1** — выбросы, их очистка и утилизация: вещества агрегируются в
  9 строк **101–109** (101 Всего; 102 твёрдые; 103 газообразные/жидкие;
  104 SO₂; 105 CO; 106 NOx; 107 углеводороды без ЛОС; 108 ЛОС; 109 прочие)
  с графами потока очистки 1–6 (отходит / без очистки / на очистные /
  уловлено-обезврежено / утилизировано / выброшено). Строки 101 и 103
  считаются автоматически.
- **Раздел 2** (специфические вещества, вне перечня — код 8888) и **Раздел 3**
  (источники загрязнения) добавлены. Титул — адресная часть с ОКУД 0609012.
- Данные очистки/источников — из `extra.tp2_air`; без них отходит=без очистки=
  выброшено. Классификация кода ЗВ в строку 102–109 — приближённая (проверяйте).

## 0.24.0 — 2026-07-10

Этап 3: 2-ТП (отходы) — точность по классу, Разделы II/III, баланс.

- **Точность массы по классу опасности** (Указания к форме): I–III класс —
  3 знака после запятой, IV–V — 1 знак; применяется и в XML (DATA_PACKET_NI),
  и в печатной форме.
- **Раздел II** (сведения региональных операторов ТКО, из `extra.tko_operators`)
  и **Раздел III** (эксплуатируемые объекты размещения/захоронения — из
  `extra.disposal_objects`: № ГРОРО, вместимость, размещено, площадь) — вместо
  прежней 3-строчной справки на стр.3.
- **Проверка баланса** масс по каждому отходу: наличие на конец года =
  приход − расход (гр.29), предупреждение при расхождении.
- Раздел I пока 18 граф; расширение до 29 граф формы Приказа Росстата № 614
  от 06.11.2025 — по официальному бланку/дистрибутиву Модуля (в докстринге).

## 0.23.0 — 2026-07-10

Этап 2: Отчёт по ПЭК переписан под форму Приказа № 173 от 15.03.2024.

- **ПЭК — структура Разделов 1–6** вместо плоских листов: Титул + Раздел 1
  (общие сведения об объектах, технологии, лаборатория с аттестатом) +
  Раздел 2 (воздух — контроль источников и наблюдения) + Раздел 3 (вода) +
  Раздел 4 (отходы — движение + контрагенты) + **Раздел 5 (побочные продукты
  производства, ППП)** + **Раздел 6 (искусственные грунты из органической
  части ТКО, с отчёта за 2025 г.)** — двух последних разделов раньше не было.
- Данные Р5/Р6 — из `extra.ppp` и `extra.artificial_soil`; при отсутствии
  выводится строка «не образуются/не производятся». XML дополнен узлами
  побочных продуктов и искусственных грунтов.
- Титул отражает форму № 173 (ред. № 262), срок 25 марта, УКЭП через ЛК.

## 0.22.0 — 2026-07-10

Переработка форм по действующим редакциям — этап 1: Декларация НВОС на
9 разделов.

- **Декларация о плате за НВОС — 9 разделов** (Приказ № 1043 в ред. № 241
  от 29.04.2025): Р1 выбросы стационарными; Р2/Р3 ПНГ на факелах (в пределах/
  сверх лимита); Р4 сбросы; Р5 отходы производства; Р6 ТКО; Р7 побочные
  продукты производства; Р8 вскрышные/вмещающие породы; Р9 побочные продукты
  животноводства. Раньше было 3 (выбросы/сбросы/отходы) с неверной нумерацией.
- Расчёт (`calc.py`) разносит плату по разделам (`by_section`), формула по
  строке не изменилась. Лист «стр.2» показывает все 9 разделов с суммами и
  **КБК** по каждому (добавлен КБК ТКО 048 1 12 01042 01 6000 120). Листы
  деталей — «Разделы 1-3 / 4 / 5-9» с колонкой «Раздел».
- Модель: у `Pollutant` — флаг `is_flare` (ПНГ, Разделы 2/3); у `WasteFlow` —
  `waste_kind` (prod/tko/byproduct/overburden/livestock; пусто — авто: ТКО
  по группе ФККО «7 3…», иначе отходы производства).
- ⚠ Точные коды строк итогового раздела бланка (Приложение 2) остаются к
  сверке; экзотические разделы Р7–Р9 заполняются при наличии данных.

## 0.21.0 — 2026-07-10

Сверка всех форм с действующими редакциями НПА (интернет-аудит), бесплатная
замена DaData, доп. коэффициент платы за 2025.

- **Бесплатное определение ОКТМО без токена DaData**: оффлайн-справочник
  `data/oktmo_ref.json` (по ключевым словам адреса, пополняется экологом,
  сверен по реальным сданным документам). Порядок: оффлайн → DaData (если
  задан токен, необязательно) → ручной ввод. Готового чистого бесплатного
  API «ОКТМО по адресу» без ключа нет (ФНС — JS-страница, Росстат — по
  сессии), поэтому основной путь — оффлайн-справочник.
- **Доп. коэффициент платы за НВОС за 2025 = 1,045** (ПП РФ №1034 от
  10.07.2025) — введён `indexation_extra_by_year` в `rates_nvos.json`;
  итоговый коэффициент 2025 = 1,32 × 1,045 = **1,3794**. Расчёт выводит
  предупреждение о составе коэффициента. ⚠ сверьте перед сдачей.
- **Исправлен срок кадастра отходов СПб**: было 1 апреля (позже реального
  дедлайна!), стало **не позднее 31 марта** (Распоряжение Комитета по
  природопользованию СПб №87-р от 26.04.2023 в ред. №28-р от 14.02.2025).
- **Актуализированы ссылки на НПА** во всех формах и в календаре обязательств
  по итогам аудита: декларация — Приказ №1043 в ред. №241 от 29.04.2025
  (9 разделов); ПЭК — форма по Приказу №173 от 15.03.2024 (не №261/№109);
  2-ТП отходы — Приказ Росстата №614 от 06.11.2025 (не №627); 2-ТП водхоз —
  Приказ №445 от 02.10.2024 (адресат — БВУ, не Росстат); журнал 1028 — ред.
  №825 от 13.12.2023; МСП/III — Приказ №30 отменён (отчётность — в составе
  ПЭК). В докстрингах форм отмечены расхождения структуры с действующими
  бланками (что ещё требует переработки).

## 0.20.0 — 2026-07-09

Декларация о плате за НВОС — печать по официальному бланку.

- **стр.1 (титульный лист)** — пронумерованные поля 1–10 официального
  бланка (вид документа, орган РПН, наименование/сокращённое, адрес,
  телефон, ИНН, КПП, ОГРН, руководитель) + строка подтверждения
  достоверности.
- **стр.2 (расчёт суммы платы)** — официальные коды строк **010–100**:
  010 ОКТМО, 020 сумма всего (= 021+022+023+024), 021 выбросы, 022 ПНГ,
  023 сбросы, 024 отходы, 030/050/070/090 — **КБК**, 040/060/080/100 —
  суммы по видам. КБК администратора 048 (Росприроднадзор) вшиты.
- Расчётные детали остались отдельными листами **Раздел 1 (выбросы) /
  Раздел 2 (сбросы) / Раздел 3 (отходы)** — ставки, коэффициенты, плата
  построчно (лист «Сводка» убран, его роль выполняет стр.2).

## 0.19.0 — 2026-07-09

Форма 2-ТП (отходы) — по официальному бланку и в реальном формате ЛКПП.

- **XML** переписан на конверт **«Модуля природопользователя» РПН**
  (`DATA_PACKET_NI`, `Program="AdiPNV"`, `DocType=3`): общий блок `ORG_INFO`
  (реквизиты + ОКВЭД + объекты `EMISS_OBJECT`) + тело `RPT_2TP_WASTE` с
  построчными `RPT_2TP_WASTE_FACT` (`WST_CODE`, `TP2_FORMING`, `TP2_TRANSF`,
  `TP2_TR_DISP` и т.д.) + `CHECKSUM`. Это формат, который импортирует
  Модуль. **ВНИМАНИЕ:** алгоритм контрольной суммы Модуля закрыт —
  `CHECKSUM` считается нашим инструментом (MD5 тела `ORG_INFO`); проверьте
  импорт в самом Модуле, при отказе он обычно пересчитывает сумму при
  повторном сохранении. ОКАТО подставляется из ОКТМО.
- **Печатная форма** — три страницы бланка Росстата: **стр.1** (титул +
  код-строка ОКУД/ОКПО/ОКВЭД/ОКТМО/ИНН/ОГРН), **стр.2** (таблица движения,
  графы А,Б,В,Г и 1–18, строки «ВСЕГО» и «Всего по N классу опасности»
  считаются автоматически), **стр.3** (справочно об объектах захоронения +
  подпись).
- Общий сборщик конверта `render/xmlutil.data_packet_ni()` — переиспользуем
  для других форм Модуля.

## 0.18.0 — 2026-07-09

Настоящие официальные бланки вместо самодельных таблиц + коэффициент
индексации 2025.

- **Журнал учёта отходов (Приказ Минприроды №1028)** — печатная форма
  переписана по официальному бланку: **Титул + Приложение 1** (состав
  образующихся отходов) **+ Приложение 2** (обобщённые данные движения,
  16 граф) **+ Приложение 3** (переданные другим лицам) **+ Приложение 4**
  (полученные). Журнал — внутренний документ, в ЛКПП не грузится, поэтому
  XML у формы больше нет (`has_xml=False`).
- **Кадастр отходов СПб** — печатная форма переписана по бланку Комитета
  по природопользованию: **Форма 1** (сведения о ЮЛ/ИП и объекте НВОС) **+
  Форма 2** (места накопления) **+ Форма 3** (движение отходов, 22 графы,
  с раздельным учётом передачи региональному оператору ТКО) **+ Форма 4**
  (объекты обработки/утилизации/обезвреживания) **+ Форма 5** (уведомление).
  Сдаётся подписанным Excel — XML убран.
- **Коэффициент индексации платы за НВОС на 2025 год = 1,32** (ПП РФ
  №1290 от 24.09.2024) внесён в `data/rates_nvos.json`. Предупреждение
  «коэффициент на 2025 не задан» больше не появляется. Для 2026 —
  примечание: старая схема «ставки 2018 × коэффициент» отменена
  (Распоряжение №2409-р от 01.09.2025), нужны новые ставки.
- Модель `WasteFlow` дополнена описательными полями (происхождение,
  агрегатное состояние, состав) и раздельными потоками (обработано,
  накопление на начало, передано на хранение/захоронение).
- Новые хелперы официальных форм в `render/xlsx` (объединение ячеек,
  точная простановка стилей по адресу A1). CLI и GUI учитывают `has_xml`.

## 0.17.0 — 2026-07-09

Выбор ИИ как в STR.RAG — панель провайдера и модели:

- Вкладка «Сервис» → **«Выбор ИИ»**: выпадающий список из 15 провайдеров
  (DeepSeek, OpenRouter, Groq, Mistral, Gemini, OpenAI, Anthropic,
  Together, xAI, VseGPT, ProxyAPI, GigaChat, YandexGPT, Ollama, LM Studio)
  с пометкой наличия ключа; выбор модели из пресетов или вручную; для
  Ollama — список реально установленных моделей; выбор **запасного
  провайдера**; кнопка «Проверить ответом».
- **Ввод API-ключа прямо в интерфейсе** — сохраняется локально
  (`~/.ecodoc/keys.json`), не нужно возиться с переменными окружения.
- Пресеты моделей на провайдера (`KNOWN_MODELS`), в т.ч. бесплатные
  модели OpenRouter (`...:free`).
- `ai setup --provider <p>` подставляет дефолтную модель провайдера.

## 0.16.0 — 2026-07-09

Скорость: облачный ИИ + параллельная обработка (было «долго»):

- **ИИ-анализ через облако** (DeepSeek/OpenRouter — ~1.5 с на документ
  против минут у локальной Ollama). `ecodoc ai setup --provider deepseek`
  теперь ставит и модель; облачные провайдеры получили дефолтные модели.
  Ollama остаётся офлайн-резервом.
- **Параллельная обработка**: извлечение текста и OCR сканов идут в
  несколько потоков (Tesseract — подпроцесс, реально ускоряется);
  запросы к облачному ИИ — до 6 одновременно. На выборке 8 документов
  ИИ-анализ занял 8 с вместо многих минут.
- OCR восстановлен к полному качеству (все страницы, 300 DPI) —
  откат ухудшения из 0.15.1; оставлен большой таймаут-предохранитель
  на страницу от зависания на битом файле.

Приватность: облачный ИИ отправляет текст документов внешнему сервису.
Для локальной работы верните Ollama: `ecodoc ai setup --provider ollama`.

## 0.15.1 — 2026-07-09

- **OCR больше не «вешает» приём на многостраничных сканах**: распознаются
  первые 20 страниц скана (реквизиты и коды — на них), 200 DPI вместо 300,
  таймаут 40 с на страницу. Раньше скан на 203 листа (проект рекультивации)
  OCR-ился ~9 минут на один файл — выглядело как зависание.
- Чанк анализа уменьшен (25 файлов без ИИ) — прогресс виден чаще на
  медленных сканах.

## 0.15.0 — 2026-07-07

Устойчивый приём тысяч файлов (тест на архиве 4105 файлов упал с
«Ошибка: undefined» и потерял 2756 принятых):

- **Сбой партии больше не рушит весь приём**: 3 попытки на партию, при
  устойчивом сбое партия пропускается, остальные загружаются; в конце —
  сколько не удалось (повторите приём только их). Раньше одна сетевая
  ошибка обнуляла весь прогресс.
- **Реестр принятых — O(n) вместо O(n²)**: `intake.json` читается и
  пишется один раз на партию (атомарно), а не на каждый файл — при
  10000 файлов это критично для скорости и целостности.
- **Анализ — чанками с прогрессом**: OCR тысяч сканов идёт долго, но
  виден прогресс «Анализ N/M», сбойный чанк не прерывает остальное.
- **Кнопка «Стоп»** — прервать загрузку/анализ, уже принятое сохранено.
- Партии мельче и с лимитом по числу файлов (≤60, ≤15 МБ) — надёжнее
  на слабой сети.
- Понятные сообщения об ошибках вместо «undefined»/«[object Object]».

## 0.14.1 — 2026-07-07

Установщик ставит ВСЁ необходимое одним запуском:

- **Python** ставится автоматически (winget), если его нет — раньше
  установщик просто прерывался.
- **7-Zip** добавлен в авто-установку (rar/7z-архивы во входящих).
- `ecodoc doctor` находит LibreOffice и 7-Zip по стандартным путям
  (не только в PATH) — показывает реальную картину.
- В конце установки печатается отчёт `doctor` — видно, что всё на месте.

Полный набор авто-установки: Python, зависимости (PDF/Excel/RTF/OCR-обёртка),
Tesseract-OCR + русский язык, LibreOffice, 7-Zip.

## 0.14.0 — 2026-07-07

Автоустановка всех внешних компонентов — «поставил и работает»:

- **Установщик сам ставит Tesseract-OCR** (через winget) и догружает
  **русский язык** в пользовательскую папку (без прав администратора),
  а также **LibreOffice** (запасное чтение старых .doc). Теперь сканы,
  фото и pdf-сканы распознаются из коробки — раньше это была ручная
  установка.
- **Приложение само находит** Tesseract и LibreOffice по стандартным
  путям (даже если их нет в PATH) и подхватывает русский язык через
  пользовательскую tessdata — OCR работает сразу после установки.
- **`ecodoc doctor`** — команда проверки: какие компоненты установлены
  (PDF/Excel/RTF-читалки, Tesseract+язык, LibreOffice, Ollama, 7-Zip).
- Понятная ошибка вместо технической, если Tesseract всё же не найден.

## 0.13.1 — 2026-07-07

- **Строгое распознавание ФККО**: раньше отходом считалось любое
  11-значное число, и из читаемых Excel-файлов набегали сотни «отходов»
  из шума — коды ОКТМО, номера строк, групповые заголовки каталога.
  Теперь код проходит проверку структуры (первая цифра 1–9, последняя =
  класс опасности 1–5, отсев круглых/повторяющихся) — мусора на ~70%
  меньше. Наименование отхода подставляется из справочника, если код
  в нём есть.
- ОКТМО по адресу: понятное сообщение вместо «Failed to fetch» —
  подсказка про бесплатный токен DaData или ручной ввод.

## 0.13.0 — 2026-07-07

По итогам приёма реального архива клиента (659 файлов):

- **Чтение Excel** (.xlsx/.xlsm через openpyxl, старый .xls через xlrd),
  **.rtf** (striprtf) и **.xml** — раньше сотни расчётов, деклараций,
  2-ТП, перечней отходов и XML-МЧД просто пропускались.
- **Длинные пути .doc**: папки площадок называются полным адресом (200+
  символов), а Word 2003 не открывает файлы с путём длиннее ~218 симв. —
  теперь .doc копируется в короткий путь перед открытием; при неудаче —
  запасной путь через LibreOffice. Длина имени папки площадки ограничена
  (полный адрес сохраняется в данных).
- **Сводка вместо потока ошибок**: не прочитанные файлы группируются по
  причине («сканы — нет Tesseract-OCR: 280», «формат .xls: 40» …) вместо
  сотен одинаковых строк.
- Пропуск временных файлов Word/Excel (`~$…`).

## 0.12.3 — 2026-07-07

- **Удаление площадки и организации** в дереве слева: наведите на строку
  — появится «✕». Удаление **безопасное**: папка (реквизиты, документы,
  отчёты) переносится в корзину рабочего пространства (`.корзина`), а не
  стирается насовсем — можно восстановить вручную. С подтверждением.
- Команды/эндпоинты `site_del`, `org_del`; workspace.delete_site/delete_org.

## 0.12.2 — 2026-07-07

- **Исправлено падение приёма `IndexError: no such group`**: регулярка
  e-mail не имела группы захвата, и на любом документе с адресом почты
  разбор рушился (проявилось на реальном пакете 658 файлов). Плюс
  `_first` теперь безопасен к паттернам без группы.
- **Устойчивость приёма**: ошибка разбора или распределения одного файла
  больше не прерывает весь пакет — файл помечается, остальные обрабатываются.

## 0.12.1 — 2026-07-05

- **Файл запуска интерфейса `ЭКО.DOC.bat`** в корне: двойной щелчок
  открывает браузер с GUI (после установки). Входит в каждый релиз;
  установщик подсказывает про него.

## 0.12.0 — 2026-07-05

Волна 4 — документы контура «Разработка»:

- **План мероприятий при НМУ** (Приказ МПР № 811) — генерация .docx:
  три режима опасности с целевым % сокращения выбросов, мероприятия
  из данных или типовой каркас, ответственные и порядок оповещения.
- **Программа ПЭК** (Приказ МПР № 109) — генерация .docx: общие сведения,
  инвентаризации, ответственные, таблица точек контроля (среда/точка/
  показатели/периодичность).
- Кнопка «Сгенерировать (.docx)» в контуре «Разработка»; команды
  `ecodoc devdoc nmu` и `ecodoc devdoc pek-program`.
- Тесты генераторов (31 passed).

## 0.11.0 — 2026-07-05

Волна 3 — рассеивание ближе к отказу от УПРЗА:

- **Площадные и линейные источники** в расчёте рассеивания: площадной
  (склад, отвал) раскладывается в сетку 4×4 точечных, линейный (дорога,
  эстакада) — в 6 точечных с делением выброса; на карте — маркеры
  исходных источников. В таблице «Рассеивание» — столбец «Тип» и
  координаты X2/Y2. Экспериментально; для экспертизы сверяйте с УПРЗА.
- Дефолт сетки карты снижен до 40×40 / 16 направлений (быстрее при
  площадных источниках; максимум концентрации меняется < 2%).
- Тесты площадных/линейных источников (28 passed).

## 0.10.0 — 2026-07-05

Волна 2 реализации roadmap:

- **2-ТП (водхоз)** реализована полностью (XML + Excel: водозабор по
  источникам, водоотведение по категориям качества, оборотная вода) —
  последняя форма отчётности перестала быть каркасом. В форме «Данные»
  — сворачиваемый блок «Водопользование».
- **Индексация ставок НВОС по годам**: `indexation_by_year` в
  rates_nvos.json (2018–2024 — по опубликованным ПП РФ; 2025–2026
  помечены к уточнению). Расчёт берёт коэффициент отчётного года и
  предупреждает, если он не задан.
- Добавлены поля **ОКПО и ОКВЭД** в форму реквизитов (нужны для 2-ТП).
- **Исправление типов из формы**: год, квартал, класс опасности
  приходили строками — сериализация приводит их к числу (иначе форма
  со сроком падала «str + int»).
- Тесты 2-ТП водхоз и коэрции типов (24 passed).

## 0.9.0 — 2026-07-05

Волна 1 реализации roadmap («делай всё»):

- **Справочник веществ и частых отходов** (`data/substances.json`, 29
  веществ + ФККО): автоподстановка в таблицах «Выбросы» и «Отходы»
  (вкладка «Данные») и в источниках рассеивания — вводите название,
  код/ПДК/класс подставляются сами.
- **Калькулятор класса опасности отхода** (Приказ МПР № 536,
  компонентный метод K=Σ(Ci/Wi)) — вкладка «Сервис».
- **Календарь — таблица со статусами-цветами** (просрочено/скоро/в срок)
  с обратным отсчётом дней вместо простого текста.
- **Определение ОКТМО по адресу** — кнопка прямо в форме реквизитов
  (через DaData, нужен токен DADATA_TOKEN).
- **Сравнение с прошлым периодом**: «Запомнить период» сохраняет снимок,
  «Сравнить с прошлым» показывает динамику образования отходов (±%).
- Тесты класса опасности и справочников (21 passed).

## 0.8.0 — 2026-07-05

Удобство интерфейса по итогам dex-ревью UX (DeepSeek+Mistral — оба
единогласно: сырой JSON во «Данных» — блокер для эколога-непрограммиста):

- **Вкладка «Данные» — форма вместо сырого JSON**: разделы «Реквизиты
  организации», «Отчётный период», редактируемые таблицы «Объекты НВОС»,
  «Выбросы и сбросы», «Отходы» (добавить/удалить строку). Правка полей
  без риска сломать структуру. Сырой JSON остался в сворачиваемом блоке
  «Расширенно» для опытных.
- **Вкладка «Рассеивание» — таблица источников** с понятными
  подписями-подсказками (H — высота трубы, w₀ — скорость ГВС и т.д.)
  вместо ручного JSON; коэффициент A подставляется сам.
- **Подсказка «что дальше»** после приёма документов: ссылки на «Данные»
  и «Генерация».
- Крупнее базовый шрифт (15px), насыщеннее акцентный и красный цвета —
  читаемость для возрастных пользователей.

## 0.7.2 — 2026-07-05

Прогон тестов приложения (17/17) + dex-ревью (DeepSeek+Mistral,
находки сверены с кодом; отклонены ложные — «двойное округление»
итогов на деле идемпотентно, у отходов корректно 2 корзины):

- **Исправлен data-flow организации**: правки реквизитов (ИНН, ОКТМО,
  адрес…) во вкладке «Данные» теперь сохраняются в org.json и
  переживают перезагрузку — раньше молча терялись при генерации.
- **Проверка контрольной суммы ИНН и ОГРН**: опечатка/ошибка OCR в ИНН
  плательщика ловится при валидации декларации (ошибка), неверный
  ОГРН — предупреждение.
- **Предупреждение о сроке сдачи**: при генерации формы — просрочка
  (со ссылкой на ст. 8.5 КоАП) или напоминание, если срок ≤ 30 дней.
- Мягкие санити-проверки в расчёте платы: коэффициент территории < 1,
  рассинхрон года ставок (если год указан в справочнике).
- Тесты: контрольные суммы реквизитов и подсказки о сроке (17 passed).

## 0.7.1 — 2026-07-05

- **Распаковка архивов при приёме**: zip — нативно (с починкой русских
  имён из cp866), rar/7z — через установленный 7-Zip; вложенные архивы
  до 2 уровней. Документы из архивов принимаются с префиксом имени
  архива для прослеживаемости.
- Защита распаковки (по итогам dex-ревью DeepSeek+Mistral, находки
  сверены с кодом): бюджет распаковки 2 ГБ и 5000 файлов (анти-zip-бомба),
  срез пути до имени файла (анти-zip-slip) + проверка containment,
  уникальные имена при совпадении basename (не затирают друг друга),
  пропуск симлинков, устойчивость к битым/зашифрованным записям,
  сбор частично распакованного при ошибке 7-Zip.
- Тесты приёма архивов (12 passed).

## 0.7.0 — 2026-07-05

По итогам теста на реальной папке (618 файлов):

- **Пакетная загрузка больших папок**: файлы уходят партиями ~20 МБ
  с прогрессом «партия i/N · принято X/Y» — исправлена ошибка
  «Invalid string length» при загрузке сотен файлов разом.
- **Площадка = полный адрес** (вместо автоматической «Основной»):
  при создании организации указывается адрес площадки (или берётся
  юрадрес из ЕГРЮЛ); рядом с выбранной площадкой — кнопка
  «🗺 на карте» (Яндекс.Карты).
- **Генерация разделена** на контуры «Отчётность» (формы для сдачи)
  и «Разработка» (проекты и документы).
- **Отчёт приёма — раскрывающиеся секции**: «Полнота данных по формам»
  и «Распознано» открыты сразу, длинное «Распределение файлов»
  свёрнуто; после загрузки видно, что принято и чего не хватает.
- Предупреждение при ИИ-анализе больших пакетов (>25 файлов) с выбором
  «с ИИ / без ИИ».

## 0.6.1 — 2026-07-04

- Брендинг: **ЭКО.DOC — продукт «ЭКО Проект»**. Атрибуция издателя в
  шапке интерфейса, README, лицензии, метаданных пакета и выводе CLI.

## 0.6.0 — 2026-07-04

Свой расчёт рассеивания вместо УПРЗА + выгрузка для УПРЗА:

- **Карта рассеивания** (`dispersion-map`, вкладка «Рассеивание» в GUI):
  суммация вкладов нескольких источников с координатами, перебор
  направлений и скоростей ветра (МРР-2017: r(u/uм), p(u/uм), поперечное
  s2), поле максимальных приземных концентраций, учёт фона, доля ПДК.
  Векторная карта (SVG) с изолинией превышения ПДК и маркерами
  источников — без внешних зависимостей.
- **Выгрузка для УПРЗА** (`upraza-export`, кнопка в GUI): Excel с листами
  «Источники»/«Выбросы»/«Инструкция» под мастер импорта «Эколога» +
  машиночитаемый JSON.
- **Проверка контрагента по ИНН** (вкладка «Сервис»): статус юрлица из
  ЕГРЮЛ + ссылка на реестр лицензий ГИС ТОР КНД (открытого API лицензий
  на отходы нет — проверка вручную по ссылке).
- **ОКТМО по адресу** через DaData (нужен бесплатный токен DADATA_TOKEN):
  автозаполнение при создании организации по ИНН; частый недостающий
  реквизит платы за НВОС.

Roadmap (осталось): линейные/площадные источники и застройка в расчёте
рассеивания; 2-ТП водхоз; API ЛКПП; кнопки принятия/отклонения
конфликтов ИИ.

## 0.5.2 — 2026-07-04

Итог многомодельного ревью (dex-дебат: DeepSeek + Mistral + Claude,
3 раунда критики, все находки сверены с кодом):

- Кэш ЕГРЮЛ на 30 дней (~/.ecodoc/egrul_cache.json) — повторный ввод
  того же ИНН мгновенный и не зависит от доступности ФНС.
- Приём: временные файлы загрузки удаляются сразу после обработки;
  отчёт каждого приёма сохраняется в attachments/приём_<дата>.txt
  (история, что и когда приносили).
- Классификация: платёжные поручения больше не распознаются
  как «договор» (порядок правил).
- Календарь: экспорт в .ics (Outlook/Google/Яндекс) с напоминаниями
  за 7 дней — кнопка в GUI и engine.export_ics().
- GUI: кнопка «Проверить всё» — валидация контекста сразу под все
  7 реализованных форм; таймауты запросов (зависший сервер больше
  не крутит спиннер вечно); лимит тела запроса 512 МБ;
  понятная ошибка при неизвестной форме.
- workspace.slug() — публичный API вместо приватного _slug.

## 0.5.1 — 2026-07-03

- **Организация по одному ИНН**: реквизиты (название, КПП, ОГРН, адрес,
  руководитель) подтягиваются из открытого сервиса ФНС (ЕГРЮЛ/ЕГРИП) —
  в GUI кнопка «Создать по ИНН», в CLI `ecodoc org add --inn 780...`.
- При создании организации автоматически создаётся площадка «Основная»
  (раньше организация без площадки «зависала» — нечего было выбрать).
- GUI: клик по организации выбирает её (и единственную площадку сразу);
  у организаций без площадок — подпись «нет площадок».
- **Загрузка папки целиком** («Папку целиком…»): берутся все поддерживаемые
  файлы (pdf/doc/docx/xls/xlsx/jpg/png/tif/txt/xml/rtf), лишнее
  пропускается с пометкой.
- **Распределение файлов при приёме**: для каждого файла — что это за
  документ (акт утилизации, протокол КХА, паспорт отхода, выписка ЕГРЮЛ,
  лицензия, журнал учёта и т.д.), какие данные из него можно взять и
  каким формам он пригодится; в конце — чего не хватает.

## 0.5.0 — 2026-07-03

- **Графический интерфейс**: команда `ecodoc gui` поднимает локальный
  веб-интерфейс (127.0.0.1:8737) и открывает браузер. Без новых
  зависимостей (stdlib http.server). Вкладки: Приём документов
  (загрузка файлов + ИИ-анализ), Данные (редактор context.json),
  Генерация (все формы, проверка/генерация/PDF в один клик),
  Календарь, Рассеивание (МРР-2017), Сервис (настройка ИИ, сторож форм).
  Организации и площадки — в боковой панели.
- Защита GUI-сервера: только 127.0.0.1, проверка Host и
  Content-Type (анти-CSRF/DNS-rebinding), открытие папок — только
  внутри рабочего пространства.
- Рабочее пространство по умолчанию — `%USERPROFILE%\ЭКО.DOC`
  (одно место данных при запуске из любой папки; $ECODOC_WORKSPACE
  и существующий ./ecodoc_workspace уважаются).

## 0.4.2 — 2026-07-03

- `ecodoc` без аргументов показывает справку и типовой сценарий
  вместо ошибки «arguments are required: cmd».

## 0.4.1 — 2026-07-03

- Установщик: `install.ps1`/`release.ps1` сохранены в UTF-8 с BOM —
  PowerShell 5.1 читал кириллицу как ANSI и падал с ParserError.
- Установщик добавляет команду `ecodoc` в PATH пользователя
  (шим в `%LOCALAPPDATA%\EcoDoc\bin`) — работает из любой папки
  в новой консоли.
- Надёжное определение Python (py/python) без разбора строки команды.

## 0.4.0 — 2026-07-02

- **ИИ-слой** (`ecodoc ai`): автообнаружение локальных моделей (Ollama —
  используются все найденные chat-модели по цепочке fallback, LM Studio)
  и 13 внешних провайдеров (Anthropic, OpenAI, OpenRouter, DeepSeek, Gemini,
  Groq, Mistral, xAI, Together, VseGPT, ProxyAPI, GigaChat, YandexGPT);
  ключи — только через переменные окружения.
- **ИИ-анализ входящих** (`intake --ai`): извлечение реквизитов, актов
  утилизации, протоколов КХА/биотеста с дословной цитатой-источником для
  каждого значения; конфликты не перезаписываются, а показываются.
- **Мульти-организации × площадки** (`org add/list`, `site add`,
  `--org/--site` во всех командах): рабочее пространство на диске,
  реквизиты организации наследуются всеми её площадками.
- **Приём документов** (`intake`): реестр принятых файлов (sha1, дата),
  отчёт «что распознано / чего не хватает / какие документы ещё нужны»
  по каждой форме.
- **Сторож форм** (`watch check`): слежение за страницами приказов, ставок
  НВОС, ЛКПП, региональных кадастров (СПб/ЛО) и локальной папкой XSD.
- **Свой расчёт рассеивания** (`dispersion`) по МРР-2017: точечные
  источники, cм/xм/uм, профиль по оси, доля ПДК — см. docs/УПРЗА_анализ.md.
- **PDF-экспорт** (`pdf`, `generate --pdf`): MS Office COM → LibreOffice →
  текстовый снимок PyMuPDF.
- Каркасы контура «Разработка»: ПНООЛР, расчёт класса опасности, НМУ,
  ПЛАРН, ДВОС, инвентаризация выбросов, программа ПЭК, ТУ.
- Индекс эталонных документов (`data/etalon_index.json`) для верификации
  генераторов и обучения ИИ-анализа — локальный файл, в репозиторий
  не входит.
- Автоустановщик `install.bat`/`install.ps1`, пакетирование `pyproject.toml`
  (команда `ecodoc`), UTF-8 вывод в консоль Windows.

## 0.3.0

- 7 полных форм отчётности (XML + Excel): декларация НВОС (с расчётом
  платы), движение отходов, ПЭК, кадастр СПб/ЛО, 2-ТП (отходы/воздух),
  отчётность III категории/МСП; каркас 2-ТП (водхоз).
- Паспорта отходов I–IV класса из ФККО; каркасы томов НДВ/НДС/СЗЗ
  с импортом из «Эколога».
- Календарь подачи + чек-лист документов; парсер входящих
  (PDF/DOCX/DOC/JPG + OCR) с провенансом.
